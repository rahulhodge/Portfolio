name: swing_plus
description: "Swing Plus Portfolio Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.2.2
  csv: ^5.0.0
  excel: ^2.0.0

flutter:
  uses-material-design: true
